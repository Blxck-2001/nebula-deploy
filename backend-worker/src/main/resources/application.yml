spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/nebula
    username: nebula
    password: nebula
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  rabbitmq:
    host: rabbitmq
    port: 5672

worker:
  work-dir: /tmp/worker

logging:
  level:
    root: INFO
