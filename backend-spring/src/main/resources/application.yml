server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/nebula
    username: nebula
    password: nebula
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672

redis:
  host: redis
  port: 6379
