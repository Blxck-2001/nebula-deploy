# backend-spring

Scaffold Spring Boot backend for Nebula Deploy.

Run with Docker Compose:

```bash
docker compose up --build backend-spring postgres rabbitmq redis
```

This scaffold includes minimal configuration for Postgres, RabbitMQ and Redis.
